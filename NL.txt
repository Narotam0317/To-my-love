<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lydia 💖</title>

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Arial', sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      color: #4a1c2f;
      text-align: center;
      overflow-x: hidden;
    }

    header {
      padding: 40px 20px 20px;
      animation: fadeIn 2s ease;
    }

    h1 {
      font-size: 2.5em;
      margin-bottom: 10px;
    }

    .love-text {
      font-size: 1.1em;
      line-height: 1.6;
      max-width: 95%;
      margin: auto;
      min-height: 70px;
    }

    .music-btn {
      margin-top: 20px;
      padding: 12px 26px;
      font-size: 1em;
      border: none;
      border-radius: 30px;
      background: #ff4f81;
      color: white;
      cursor: pointer;
      box-shadow: 0 5px 15px rgba(0,0,0,0.3);
    }

    .gallery {
      display: flex;
      flex-direction: column;
      gap: 25px;
      padding: 30px 15px;
    }

    .gallery img {
      width: 100%;
      max-width: 320px;
      margin: auto;
      border-radius: 20px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.25);
      animation: fadeUp 1.5s ease forwards;
      opacity: 0;
    }

    .gallery img:nth-child(1) { animation-delay: 0.2s; }
    .gallery img:nth-child(2) { animation-delay: 0.5s; }
    .gallery img:nth-child(3) { animation-delay: 0.8s; }

    footer {
      padding: 30px 10px;
      font-size: 1.1em;
      opacity: 0.9;
    }

    /* Floating hearts */
    .heart {
      position: fixed;
      bottom: -20px;
      font-size: 20px;
      animation: rise linear infinite;
      opacity: 0.7;
    }

    @keyframes rise {
      from {
        transform: translateY(0) scale(1);
        opacity: 1;
      }
      to {
        transform: translateY(-120vh) scale(1.5);
        opacity: 0;
      }
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    @keyframes fadeUp {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body>

  <audio id="bgMusic" loop>
    <source src="music.mp3" type="audio/mpeg">
  </audio>

  <header>
    <h1>Lydia 💕</h1>

    <div class="love-text" id="typeText"></div>

    <button class="music-btn" onclick="toggleMusic()">▶ Play Our Song</button>
  </header>

  <section class="gallery">
    <img src="photo1.jpg" alt="Memory with Lydia">
    <img src="photo2.jpg" alt="Another memory with Lydia">
    <img src="photo3.jpg" alt="Us together">
  </section>

  <footer>
    Forever yours 💞 <br>
    <small>Since always 💖</small>
  </footer>

  <script>
    /* Music toggle */
    const music = document.getElementById("bgMusic");
    const btn = document.querySelector(".music-btn");

    function toggleMusic() {
      if (music.paused) {
        music.play();
        btn.innerText = "⏸ Pause Our Song";
      } else {
        music.pause();
        btn.innerText = "▶ Play Our Song";
      }
    }

    /* Typing effect */
    const text = "I love you soooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooooo much my sugar plum 💖💖💖";
    let i = 0;
    function typeEffect() {
      if (i < text.length) {
        document.getElementById("typeText").innerHTML += text.charAt(i);
        i++;
        setTimeout(typeEffect, 40);
      }
    }
    typeEffect();

    /* Floating hearts generator */
    setInterval(() => {
      const heart = document.createElement("div");
      heart.className = "heart";
      heart.innerHTML = "💖";
      heart.style.left = Math.random() * 100 + "vw";
      heart.style.animationDuration = (5 + Math.random() * 3) + "s";
      document.body.appendChild(heart);

      setTimeout(() => heart.remove(), 8000);
    }, 800);
  </script>

</body>
</html>
